import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

export default function CalendarList() {
  return (
    <View>
      <Text>CalendarList</Text>
    </View>
  );
}

const styles = StyleSheet.create({});
